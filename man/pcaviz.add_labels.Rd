% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcaviz.R
\name{pcaviz.add_labels}
\alias{pcaviz.add_labels}
\title{Add Labels to Points in PCA Scatter Plot}
\usage{
pcaviz.add_labels(
  this,
  labels,
  x = NULL,
  y = NULL,
  type = c("text", "label"),
  max.overlaps = 20
)
}
\arguments{
\item{this}{A \code{pcaviz} object representing the PCA visualization.}

\item{labels}{A character vector of labels corresponding to each point in the plot.}

\item{x}{An optional numeric vector specifying the x-coordinates of the points.
If \code{NULL}, defaults to the first principal component (CP1).}

\item{y}{An optional numeric vector specifying the y-coordinates of the points.
If \code{NULL}, defaults to the second principal component (CP2).}

\item{type}{A character string specifying the labeling style: \code{"text"} or \code{"label"}.}

\item{max.overlaps}{An integer setting the maximum number of overlapping labels allowed. Defaults to \code{20}.}
}
\value{
A modified \code{pcaviz} object with added labels.
}
\description{
This function enhances a PCA scatter plot by adding text labels to the points,
allowing for easier identification and interpretation of the plotted data.
}
\details{
The function validates that the length of \code{labels} matches the number of points
in the PCA plot. If \code{x} or \code{y} are \code{NULL}, they default to the PCA components.
}
